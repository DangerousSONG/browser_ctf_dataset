services:
  main:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BENCH_V8_IMAGE: {image_tag}
    image: browser-v8-{task_slug}
    working_dir: /workspace
    mem_limit: 16g
    cpus: 8.0
    cap_drop:
      - ALL
    cap_add:
      - SETUID
      - SETGID
    security_opt:
      - no-new-privileges:true
